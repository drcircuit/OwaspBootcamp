<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mission Control - NotSoAnonymous Portal</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <div class="header-content">
                <div class="logo">
                    <span class="logo-icon">🎭</span>
                    <h1>NOTSO<span style="color: var(--neon-pink);">ANONYMOUS</span></h1>
                </div>
                
                <div class="target-info">
                    <span class="target-label">TARGET:</span>
                    <span class="target-name">Evil Capitalistic Corp</span>
                </div>
                
                <nav>
                    <a href="/dashboard">Dashboard</a>
                    <a href="/logout">Logout</a>
                </nav>
            </div>
        </header>
        
        <main>
            <div class="hero">
                <h1>🔥 MISSION CONTROL 🔥</h1>
                <p>Welcome back, <span class="hacker-alias"><%= user.hacker_alias || user.username %></span></p>
                <p style="color: var(--warning-red);">Your mission: Expose Evil Capitalistic Corp's security failures</p>
            </div>
            
            <!-- Progress Overview -->
            <section class="progress-section">
                <h2 style="font-family: 'Orbitron', monospace; color: var(--neon-blue); margin-bottom: 20px;">
                    📊 OPERATION PROGRESS
                </h2>
                
                <div class="progress-stats">
                    <div class="stat-card">
                        <div class="stat-value"><%= progress.completed %></div>
                        <div class="stat-label">Challenges Pwned</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-value"><%= progress.total %></div>
                        <div class="stat-label">Total Challenges</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-value"><%= progress.points %></div>
                        <div class="stat-label">Reputation Points</div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-value"><%= progress.percentage %>%</div>
                        <div class="stat-label">Mission Complete</div>
                    </div>
                </div>
                
                <div class="progress-bar-container">
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: <%= progress.percentage %>%"></div>
                    </div>
                    <div class="progress-text">
                        <% if (progress.percentage === 100) { %>
                            🎉 MISSION ACCOMPLISHED! <a href="/victory">Claim Your Diploma</a>
                        <% } else { %>
                            <%= progress.completed %> / <%= progress.total %> Challenges Completed
                        <% } %>
                    </div>
                </div>
            </section>
            
            <!-- OWASP Topics -->
            <section>
                <h2 style="font-family: 'Orbitron', monospace; color: var(--neon-blue); margin-bottom: 20px;">
                    🔒 OWASP TOP 10 TOPICS
                </h2>
                <p style="color: var(--neon-blue); margin-bottom: 25px;">
                    Each topic contains a natural progression: Example → Lab 1 (Easy) → Lab 2 (Medium) → Lab 3 (Hard)
                </p>
                
                <div class="stages-grid">
                    <% topics.forEach(topic => { %>
                        <a href="/topic/<%= topic.owasp_category %>" style="text-decoration: none;">
                            <div class="stage-card">
                                <div class="stage-header">
                                    <span class="stage-icon">🔒</span>
                                    <h3 class="stage-title"><%= topic.owasp_category %></h3>
                                </div>
                                <p class="stage-description">
                                    <% if (topic.owasp_category === 'A01') { %>
                                        Broken Access Control - IDOR and privilege escalation
                                    <% } else if (topic.owasp_category === 'A02') { %>
                                        Security Misconfiguration - Debug endpoints and exposed configs
                                    <% } else if (topic.owasp_category === 'A03') { %>
                                        Supply Chain Failures - Vulnerable dependencies
                                    <% } else if (topic.owasp_category === 'A04') { %>
                                        Cryptographic Failures - Weak hashing and plaintext storage
                                    <% } else if (topic.owasp_category === 'A05') { %>
                                        Injection - SQL injection and data extraction
                                    <% } else if (topic.owasp_category === 'A06') { %>
                                        Insecure Design - Business logic flaws
                                    <% } else if (topic.owasp_category === 'A07') { %>
                                        Authentication Failures - Weak passwords and session hijacking
                                    <% } else if (topic.owasp_category === 'A08') { %>
                                        Integrity Failures - Unsigned updates and checksums
                                    <% } else if (topic.owasp_category === 'A09') { %>
                                        Logging Failures - Missing logs and data leaks
                                    <% } else if (topic.owasp_category === 'A10') { %>
                                        Exception Mishandling - Stack traces and information disclosure
                                    <% } else if (topic.owasp_category === 'ALL') { %>
                                        Final Exam - All vulnerabilities, no hints
                                    <% } %>
                                </p>
                                <div class="stage-progress">
                                    <span class="challenge-count">
                                        <%= topic.total_challenges %> Challenges
                                    </span>
                                    <span class="completed">
                                        <% 
                                        const completed = parseInt(topic.completed_challenges) || 0;
                                        const total = parseInt(topic.total_challenges) || 0;
                                        %>
                                        <%= completed %> / <%= total %> ✓
                                    </span>
                                </div>
                            </div>
                        </a>
                    <% }) %>
                </div>
            </section>
            
            <div class="mt-3 text-center" style="padding: 30px; background: rgba(255, 0, 110, 0.1); border: 2px solid var(--neon-pink); border-radius: 8px;">
                <h3 style="color: var(--neon-pink); font-family: 'Orbitron', monospace; margin-bottom: 10px;">
                    💡 LEARNING PATH
                </h3>
                <p style="color: var(--primary-green);">
                    Each OWASP topic follows its own natural progression for that vulnerability type.
                    <br>
                    Start with the <strong>Example</strong> to learn, then progress through <strong>3 Labs</strong> of increasing difficulty.
                    <br>
                    Use real tools like Browser DevTools, Burp Suite, curl, and sqlmap!
                </p>
            </div>
        </main>
        
        <footer>
            <p>🎭 NotSoAnonymous Collective - Fighting Corporate Security Theater Since 2025</p>
            <div class="footer-warning">
                <strong>⚠️ EDUCATIONAL PURPOSES ONLY</strong><br>
                These are deliberately vulnerable applications for learning. Never use these techniques on systems you don't own!
            </div>
        </footer>
    </div>
</body>
</html>
